beforeAll(() => {
  // eslint-disable-next-line no-console
  console.log('add global setup code here');
});


// test altering basic query response
describe('Chaos 🐨  Agent Unit Tests', () => {
  // note the async so we can await fetch
  test('add unit tests here', () => {
    expect('the more tests the merrier').toBe('the more tests the merrier');
  });
});


afterAll(() => {
  // eslint-disable-next-line no-console
  console.log('add global clean up code here');
});
